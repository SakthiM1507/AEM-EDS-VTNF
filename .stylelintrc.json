{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "selector-class-pattern": [
      "^[a-z][a-z0-9]*([_-]{1,2}[a-z0-9]+)*$",
      {
        "message": "Expected class selector to be lowercase, with '-', '--', '_' or '__' separators (e.g. block_element--modifier)"
      }
    ]
  }
}
